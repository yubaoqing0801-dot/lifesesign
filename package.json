{
  "name": "lifedesign",
  "version": "1.0.0",
  "description": "人生设计系统 - 个人成长与目标管理",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npx vite build && node ../fix-build.js",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}